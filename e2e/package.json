{
  "name": "mahad-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end smoke suite for Mahad Nafsy",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "otplib": "^13.4.0"
  }
}
