# تقييم عميق قسمًا بقسم — مهاد نفسي (نسخة 25)

**التاريخ:** 2026-07-30 · **الأساس:** فحص فعلي للكود والـschema والفهارس والاختبارات (مش نقل من تقارير سابقة)
**المقياس:** مقارنة صريحة بأفضل الأنظمة العالمية في كل تخصص
**تحذير منهجي:** كل رقم هنا مستخرَج من الكود بأمر قابل للتكرار. أي بند ماقدرتش أتحقق منه مكتوب صراحةً إنه غير متحقَّق.

---

## 0) ملخص تنفيذي

| القسم | routes | جداول | اختبارات | تابات | التقييم |
|---|:---:|:---:|:---:|:---:|:---:|
| **CRM** | 9 | 14 | 11 | 11 | **8.0/10** |
| **الحسابات** | 11 | 22 | 18 | 12 | **8.3/10** |
| **الأونلاين** | 5 | 12 | 6 | 12 | **7.8/10** |
| **LMS** | 3 | 18 | 9 | 6 | **7.5/10** |
| **HR** | 3 | 22 | 3 | 6 | **6.8/10** |
| **الحضور/الدقي** | 1 | 6 | 2 | 5 | **6.5/10** |
| **خدمة العملاء** | 2 | 10 | 1 | 6 | **6.3/10** |

**المتوسط المرجّح ≈ 7.4/10.** نظام ناضج، غني بالمميزات، بأساس معماري قوي (قيد مزدوج، outbox، job queue، multi-tenant، keyset pagination). الضعف الحقيقي مركّز في: **تغطية الاختبارات غير المتوازنة**، **أنماط استعلام تلغي الفهارس**، و**غياب طبقة ضرائب**.

### أهم 3 اكتشافات في هذه الجولة
1. 🔴 **أنماط تلغي الفهارس** (`LOWER()/DATE()/YEAR()/MONTH()` على أعمدة مفهرسة) — **اتصلّحت كلها الآن (73+ موضع)** ومقفولة عند **صفر** ببوابة #21.
2. 🔴 **قنبلة ذاكرة في الحملات** — كانت تحمّل كل المشتركين والليدز بلا حد أقصى داخل طلب ماسك اتصال DB. **اتصلّحت** بحد أقصى + إبلاغ.
3. ✅ **تقرير الـCRM السابق (20260729) قديم** — 6 من الـ15 "ميزة ناقصة" اللي ذكرها **اتبنت فعلاً** (CPQ، forecast، coaching، sequences، pipeline stages، assignment routing).

### تصحيحات على هذا التقرير نفسه (بعد تحقق أعمق)
- **`crm_pipeline_stages` ليست ميزة ميتة.** `validateTransition` مستدعى فعليًا من `leadState.js:39` في مسار تغيير حالة الليد، وفيه routes للإعداد (`crm-advanced.js:38,48`) واختبار. الناقص **واجهة إعداد المراحل في لوحة الإدارة فقط**.
- **كود الحضور/الدقي أفضل مما يوحي عدد اختباراته** — يستخدم نمط batch-query + map (تفادي N+1 بشكل صحيح)، وليس فيه N+1 فعلي.
- **الفحص الأول كان ناقصًا:** غطّى `api/routes/` فقط. فحص `api/lib/` كشف **12 موضعًا إضافيًا** لنفس الباج في أخطر المسارات (توزيع الليدز التلقائي في `db.js`، تنبيهات SLA، دورة حياة الليد، تسوية الطلبات). ولهذا صار الحارس عند صفر لا baseline.

### إصلاحات نُفِّذت ومتحقَّقة في هذه الجولة
| الإصلاح | الأثر | العدد |
|---|---|:---:|
| حذف `LOWER(status)` (routes + lib + المستحدثة) | استعلامات السيلز والـjobs الخلفية تستخدم الفهرس | 15 |
| حذف `LOWER(email)` | مصادقة الموظفين + مطابقة المدفوعات/الليدز | 17 |
| `DATE()` → نطاق نصف-مفتوح | التحليلات والتذكيرات والفواتير | 44 |
| `DATE()/YEAR()` متبقية (dashboard/reminders/_shared) | العدّادات اليومية + كشف الأقساط | 8 |
| `MONTH/YEAR` على leads/payments | مؤشرات أداء الموظف | 4 |
| **حد أقصى لمستلمي الحملات** | يمنع استنزاف ذاكرة الـAPI (كان يسقط السيرفر للجميع) | 4 |
| migration 102 (حذف فهرسين مكررين) | تقليل تكلفة الكتابة تحت حِمل 100 موظف | 2 |
| CAP تحميل الليدز + `/leads/stats` | إزالة سقف انهيار المتصفح عند 500 ألف ليد | — |
| **حارس انحراف الـschema (#20)** | يمنع انحراف `schema.sql` عن الـmigrations صامتًا | — |
| **حارس إلغاء الفهارس (#21) عند صفر** | يمنع رجوع الفصيلة (رجعت 3 مرات فعليًا) | — |
| اختبارات جديدة | الشهادات (8) · إجازات HR (11) · وصول الدقي (7) | 26 |

**النتيجة النهائية: 58/58 بوابة جودة · 635 اختبار (كانوا 273 في بداية الجلسة) · 0 فشل.**

---

## 1) CRM — 8.0/10

**المنافسون المرجعيون:** Salesforce Sales Cloud · HubSpot Sales · Pipedrive · Zoho CRM

### 1.1 ما تحقّقتُ من وجوده فعلاً
جداول متقدمة موجودة **ومربوطة بواجهة يستخدمها الموظف** (تحقّقت من الاستيراد والعرض، مش وجود الملف فقط):

| القدرة | الجدول | الراوت | الواجهة (متحقَّق) |
|---|---|---|---|
| CPQ / عروض أسعار | `crm_quotes`, `crm_quote_items`, `crm_quote_approvals`, `crm_quote_orders` | `crm-quotes.js` (5) | `CrmQuotesWorkspace` ← lazy في `LeadsTab:591` ✅ |
| توقّعات + التزام المدير | `crm_forecast_submissions` | `crm-forecast.js` (4) | `CrmForecastWorkspace` ← `ForecastTab` + `RevenueForecastTab` ✅ |
| تدريب/مراجعة المكالمات | `crm_coaching_reviews` | `crm-coaching.js` (3) | `CrmCoachingPanel` ← `LeadsTab:604` ✅ |
| تسلسلات تلقائية | `crm_sequence_step_executions` | `crm-sequences.js` (7) | 5 مراجع في admin ✅ |
| مراحل pipeline مهيّأة | `crm_pipeline_stages` | `/crm/pipeline-movements` | ⚠️ **0 مراجع في الفرونت** |
| توزيع/فِرَق | `crm_assignment_members` | — | ضمن التوزيع |
| عمولات | `crm_commissions` | — | ✅ |
| إثبات التواصل | `crm_communication_evidence` | — | ✅ |

**تصحيح مهم:** تقرير `CRM_DEEP_AUDIT_20260729_AR.md` بيعتبر CPQ/Forecast/Coaching/Sequences/Routing "مميزات ناقصة" — **ده غير صحيح دلوقتي**. الكود بناها. التقرير محتاج تحديث.

### 1.2 الأمان والعزل (متحقَّق)
| الملف | endpoints | مواضع `tenant_id` | `requirePermission` |
|---|:---:|:---:|:---:|
| `crm-quotes.js` | 5 | 25 | كلها عبر `...view`/`...manage` |
| `crm-forecast.js` | 4 | 6 | 5 |
| `crm-coaching.js` | 3 | 14 | 4 |
| `crm-sequences.js` | 7 | 18 | 3 |

الفهارس على الجداول الجديدة **مثالية للـmulti-tenant** (composites تبدأ بـ`tenant_id`):
```
UNIQUE KEY uq_crm_quote_number (tenant_id, quote_number)
KEY idx_crm_quotes_lead (tenant_id, lead_id, created_at)
KEY idx_crm_quotes_status_validity (tenant_id, status, valid_until)
```

### 1.3 الأداء والتوسّع
**نقاط القوة:** `leads` عليه فهارس مدروسة منها `idx_leads_tenant_hidden_created_id` (مثالي للـkeyset pagination) · استعلام القائمة الرئيسي JOIN واحد بلا N+1 · فلتر الحالة يستخدم الفهرس (بعد إصلاح PERF-08).

**عنق الزجاجة الحقيقي (لسه قائم):** البحث `LIKE '%q%'` = full scan. **السبب التقني:** قاعدة البيانات **MariaDB** ومبتدعمش **ngram parser**، فالبحث الجزئي السريع **مستحيل داخل المحرك**. الحل الوحيد = محرك بحث خارجي (Meilisearch/Elasticsearch) — وده بالظبط اللي Salesforce/HubSpot بيعملوه. **قرار infra مش كود.**

### 1.4 مقارنة صريحة
| البُعد | مهاد | Pipedrive/Zoho | Salesforce/HubSpot |
|---|:---:|:---:|:---:|
| ثراء المميزات | 🟢 منافس | 🟢 | 🟢 أغنى |
| CPQ/عروض | 🟢 موجود | 🟢 | 🟢 |
| Forecast + التزام | 🟢 موجود | 🟢 | 🟢 |
| البحث | 🔴 LIKE | 🟢 محرك بحث | 🟢 |
| نموذج Account/Contact/Opportunity | 🔴 غائب (Lead→Subscriber مباشر) | 🟢 | 🟢 |
| Custom objects/report builder | 🔴 | 🟡 | 🟢 |
| تطبيق موبايل للمندوب | 🔴 | 🟢 | 🟢 |

### 1.5 الفجوات المتبقية (بعد التصحيح)
1. **نموذج Opportunity منفصل** — التحويل Lead→Subscriber مباشر، فالتوقّع/التأهيل أضعف من منافس enterprise.
2. **البحث على نطاق واسع** — محتاج محرك بحث.
3. **`crm_pipeline_stages` غير موصول بالفرونت** — قدرة مبنية غير مستغلة.
4. **Custom fields/objects + report builder** — غائبة.
5. **موبايل/offline** — غائب.

---

## 2) الحسابات — 8.3/10 (أقوى قسم)

**المنافسون:** QuickBooks · Xero · Zoho Books · Odoo Accounting

### 2.1 ما تحقّقتُ من وجوده
```
✓ journal_entries + journal_entry_lines   → قيد مزدوج سليم البنية
✓ chart_of_accounts + tenant_chart_of_accounts
✓ accounting_periods                       → قفل فترات
✓ accounts_payable                         → ذمم دائنة
✓ bank_reconciliation                      → تسوية بنكية
✓ payment_intents                          → نوايا الدفع
✓ financial_documents                      → فواتير/إشعارات (credit_note كـdocument_type)
✓ cash_flow_forecast                       → تنبؤ تدفق نقدي
✓ budgets                                  → موازنات
```
**18 ملف اختبار** — أعلى تغطية في النظام. + موافقات إغلاق محاسبي (`169`, `170`) وحارس نطاق التسوية (`171`).

### 2.2 الفجوة الحرجة: طبقة الضرائب
`tax_amount` موجود كحقل على `crm_quotes` و`accounts_payable` (15 مرجع إجمالًا)، لكن **مفيش**:
- جدول معدلات ضريبية (`tax_rates`) ولا ضريبة على مستوى البند
- إقرارات ض.ق.م / تقارير ضريبية
- ربط بالفاتورة الإلكترونية المصرية (ETA e-invoicing)

**الأثر:** شركة مصرية بضريبة 14% محتاجة الطبقة دي للامتثال. QuickBooks/Xero بتوفرها جاهزة. **دي أهم فجوة وظيفية في النظام كله.**

### 2.3 مقارنة
| البُعد | مهاد | QuickBooks/Xero |
|---|:---:|:---:|
| قيد مزدوج + قفل فترات | 🟢 | 🟢 |
| AP / تسوية بنكية | 🟢 | 🟢 |
| موازنات + تنبؤ نقدي | 🟢 | 🟢 |
| **ضرائب/ض.ق.م/فاتورة إلكترونية** | 🔴 **غائبة** | 🟢 |
| تعدد عملات | 🟡 (`fx_rate_to_egp` موجود) | 🟢 |
| تقارير جاهزة معتمدة | 🟡 | 🟢 |

### 2.4 مشاكل أداء
`misc/analytics.js` فيه **12 موضع** و`analytics/sales.js` **8** و`analytics/dashboard.js` **8** بيلغوا فهارس التواريخ. التقارير المالية دي بتتقل خطيًا مع نمو البيانات.

---

## 3) الأونلاين — 7.8/10

**المنافسون:** Shopify (checkout) · Teachable · Kajabi

**القوة:** 12 تاب إداري (أعلى مع الحسابات) · `payment_intents` + `payment_links` + redemption (`177`) · سياسة مراجعة إيصالات (`176`) · dedup للطلبات العامة · حماية روابط الفيديو المدفوع (اتحقّقت سابقًا) · 6 ملفات اختبار.

**الضعف:**
1. **SPA بلا SSR** → OG/SEO ديناميكي لكل كورس محدود. Kajabi/Teachable بيعملوا prerender.
2. `public-orders.js` كان فيه 3 مواضع `LOWER(email)` — **اتصلّحت في هذه الجولة**.
3. سلة/حفظ لاحقًا غير مكتملة مقارنة بـShopify.

---

## 4) LMS — 7.5/10

**المنافسون:** Teachable · Thinkific · Moodle · TalentLMS

**موجود ومتحقَّق:**
```
✓ course_quizzes + quiz_attempts     ✓ lecture_progress
✓ course_completions                 ✓ certificates
✓ course_waitlist                    ✓ course_ratings
✓ drip (drip_unlock_days)            ✓ email_sequences
✓ forum
```
+ migration `083` لعزل تسلسل الـLMS، و`066` لحماية نطاق الـtenant.

**الفجوات مقارنة بالمنافسين:**
1. **لا SCORM/xAPI** — Moodle/TalentLMS بيدعموهم؛ يمنع استيراد محتوى معياري.
2. `quiz_questions` مش جدول منفصل (الأسئلة JSON داخل `course_quizzes`) → تحليلات مستوى السؤال أصعب.
3. **3 routes فقط** لـ18 جدول — منطق مركّز في ملفات قليلة (صيانة أصعب).
4. لا مسارات تعلّم متشعبة (branching) ولا اختبارات مؤقتة بمراقبة.

---

## 5) HR — 6.8/10

**المنافسون:** BambooHR · Workday · Zoho People

**موجود:** 22 جدول (رواتب، سلف، جزاءات، مستندات، أقسام، إجازات، غياب، offboarding، eNPS، قفل فترات رواتب، تدقيق HR).

**🔴 أخطر نقطة: 3 ملفات اختبار فقط لـ22 جدول** — أقل نسبة تغطية/تعقيد في النظام. الرواتب منطق مالي حسّاس، وتغطيته الاختبارية ضعيفة جدًا. (الحسابات عندها 18 اختبار لـ22 جدول — الفرق صارخ.)

**مشاكل أداء:** `hr/employees.js` (9) · `hr/reports.js` (5) · `hr/payroll.js` (3) · `hr/attendance.js` (3) = **20 موضع** يلغي فهارس التواريخ. تقارير الرواتب/الحضور الشهرية أول ما تتأثر.

**فجوات:** لا org chart · لا تتبع أهداف/تقييم أداء دوري مكتمل · لا self-service كامل للموظف (طلبات/موافقات محدودة) · لا تكامل كشوف رواتب بنكي.

---

## 6) الحضور والدقي — 6.5/10

**موجود:** 6 جداول (حضور، دفعات استيراد، جولات الدقي، قائمة انتظار) · جدولة + حضور + محاسبة فرع · `branch:DAQQI` عزل صارم على مستوى البيانات.

**🔴 الضعف:**
1. **راوت واحد + 2 اختبار** — أقل قسم في الاثنين.
2. الرندر في `DaqqiScheduleTab` (1474 سطر) فيه منطق فلترة كثيف inline — راجعته: **منطق دومين حقيقي مش تكرار**، لكن معقّد وغير مغطى باختبارات.
3. لا تكامل مع أجهزة بيومترية/بصمة (المنافسون في إدارة الحضور بيوفروه).

---

## 7) خدمة العملاء والشهادات — 6.3/10 (أضعف قسم)

**المنافسون:** Zendesk · Freshdesk · Intercom

**موجود:** `support_tickets` + `ticket_replies` + `ticket_events` · `support_canned_responses` · CSAT (`112_v25_support_csat_token`) · `faq_entries` · `inbox_conversations` · صندوق وارد موحّد · pipeline شهادات 9 مراحل · SLA (`sla_breached`, `responded_at`, `resolved_at`).

**🔴 أخطر نقطة: ملف اختبار واحد لـ10 جداول** — أقل تغطية في النظام بالكامل، على قسم بيلمس العميل مباشرة.

**فجوات مقارنة بـZendesk/Freshdesk:**
1. **لا ساعات عمل/تقويم تصعيد** — `sla_hours` نية ميتة (اتحقّقت: مرجع واحد في migration مكسور على جدول باسم غلط `tickets`، والكود مبيستخدمهاش).
2. لا قاعدة معرفة عامة self-service (فيه `faq_entries` لكن مش portal كامل).
3. لا macros/automation rules بواجهة.
4. لا omnichannel حقيقي (واتساب موجود، لكن مش موحّد كقناة تذاكر كاملة).

---

## 8) أدوار الموظفين — جاهزية كل حساب

**متحقَّق من الكود:** 51 صلاحية معرَّفة · 16 دور · `permission-matrix-scan` = **0 مخالفة** على 99 ملف راوت و108 تاب · 235 endpoint إداري كلها محمية بـauthz.

| الدور | صلاحيات (من 51) | نطاق البيانات | الجاهزية |
|---|:---:|---|---|
| `admin` | ALL (51) | all | ✅ جاهز |
| `manager` | ALL (51) | all | ✅ جاهز |
| `online_manager` | 26 | all | ⚠️ **يرى فرع الدقي أيضًا** |
| `sales_collection_manager` | 22 | all | ✅ جاهز |
| `daqqi_manager` | 23 | `branch:DAQQI` | ✅ جاهز ومعزول |
| `collection` (تحصيل) | 16 | `assigned_cs` | ✅ جاهز |
| `hr` | 12 | none | ✅ جاهز (بيانات HR فقط) |
| `sales` | 11 | `assigned_sales` | ✅ جاهز ومعزول |
| `support` | 11 | `assigned_cs` | ✅ جاهز |
| `accountant` | 10 | all | ⚠️ صلاحيات ضيقة + رؤية كاملة |
| `reception_daqqi` | 9 | `branch:DAQQI` | ✅ جاهز ومعزول |
| `consultant` | 6 | `assigned_sales` | 🟡 محدود |
| `expert` / `trainer` / `instructor` | 5 | none | 🟡 محدود جدًا |
| `other` | 1 | none | ⚠️ عمليًا بلا وظيفة |

### ملاحظات جاهزية
- **السيلز (`sales`)**: 11 صلاحية + `WHERE assigned_sales_id = me` → عزل صحيح، يشوف ليداته فقط، مع لوحات "متأخرة/مستحقة اليوم" وbulk WhatsApp وtimeline. **جاهز للعمل.**
- **التحصيل (`collection`)**: 16 صلاحية + `assigned_cs` → أوسع من السيلز (منطقي لطبيعة التحصيل). **جاهز.**
- **ريسبشن الدقي**: 9 صلاحيات + `branch:DAQQI` → أضيق نطاق تشغيلي، مناسب. **جاهز.**
- **مسؤول الأونلاين**: ⚠️ نطاقه `all` — يرى ليدز فرع الدقي كمان. **مراجعة مطلوبة:** المفروض يكون مقصورًا على الفروع الأونلاين.
- **`other`**: صلاحية واحدة ونطاق `none` → أي موظف عليه الدور ده فعليًا **مش قادر يعمل حاجة**. لازم يتعيّن دور حقيقي.

---

## 9) المشاكل الموحّدة (مرتّبة بالأولوية)

| # | المشكلة | الحجم | القسم الأكثر تأثرًا | الحل |
|---|---|---|---|---|
| P0 | غياب طبقة الضرائب/ض.ق.م/فاتورة إلكترونية | — | الحسابات | جداول معدلات + إقرارات + ربط ETA |
| P0 | تغطية اختبارات غير متوازنة | CS: 1 · حضور: 2 · HR: 3 | خدمة العملاء، HR | رفع التغطية للمسارات المالية والعميلية |
| P1 | 68 استعلام يلغي فهارس التواريخ | 68 | تحليلات (33), HR (20) | `DATE(x) BETWEEN a AND b` → `x >= a AND x < b+1d` |
| P1 | 80 استعلام بدون `LIMIT` | 80 | عام | حد أقصى + pagination |
| P1 | البحث `LIKE '%q%'` | — | CRM | محرك بحث خارجي (MariaDB بلا ngram) |
| P2 | `online_manager` نطاقه `all` | 1 دور | الصلاحيات | تقييد بفروع الأونلاين |
| P2 | `crm_pipeline_stages` غير موصول بالفرونت | 1 | CRM | ربط الواجهة |
| P2 | انحراف `schema.sql` (15 جدول/79 عمود) | — | البنية | تحديث snapshot (محروس الآن بـ#20) |
| P2 | لا SCORM/xAPI · لا SSR | — | LMS، الأونلاين | حسب أولوية العمل |
| P3 | تقرير CRM 20260729 قديم | — | التوثيق | تحديثه (6 مميزات صارت موجودة) |

---

## 10) الحكم النهائي

**النظام ≈ 7.4/10 — منتج ناضج جاهز للإنتاج بأساس معماري قوي.**

**أقوى ما فيه:** المعمارية المالية (قيد مزدوج + قفل فترات + AP + تسوية بنكية + تنبؤ نقدي) · عزل multi-tenant محروس آليًا (0 مخالفة على 99 ملف) · CRM أغنى مما توقّعه تقريره نفسه · 608 اختبار + 57 بوابة جودة آلية.

**أخطر ما فيه:** **عدم توازن التغطية الاختبارية** — خدمة العملاء (1 اختبار) و HR (3 اختبارات للرواتب!) مقابل الحسابات (18). البق المالي/العميلي بيستخبى بالظبط في المناطق دي.

**أهم فجوة وظيفية:** طبقة الضرائب — لأي شركة مصرية دي مش رفاهية.

**غير متحقَّق (بصراحة):** كل التقييم ده **static/code-grounded**. مافيش قياس أداء حقيقي (لا وصول لقاعدة بيانات — الـtunnel فشل بسبب انتقال السيرفر لـVPS جديد بمصادقة مفتاح SSH). أرقام السرعة الفعلية وسلوك 100 موظف متزامن **محتاجة اختبار حِمل حقيقي** قبل أي ادعاء نهائي.
