# تقرير QA شامل للإنتاج - 30 جزء

التاريخ: 2026-06-10  
النطاق: الموقع العام `mahadnafsy.com`، لوحة الإدارة `admin.mahadnafsy.com`، API، قاعدة البيانات، ومسار العميل من التسويق حتى التحصيل والحسابات.

## ملخص تنفيذي

- تم إصلاح ظهور عملاء الدقي والحسابات: الدقي من `4 / 4` إلى `9 / 9`، ومحاسبة الدقي من `13,300 ج` قبل backfill الكامل إلى `14,200 ج`.
- تم إصلاح رابط `mahadnafsy.com/join-us` الذي كان يعرض 404.
- تم إصلاح تضارب `client` بين `/new/` والجذر `/` عبر جعل `base` و`Router basename` قابلين للإعداد.
- تم Backfill آمن على الإنتاج بعد backup:
  - `subscribers.branch`: تم تحديث 41 سجل.
  - `payments.branch`: تم تحديث 18 دفعة.
  - backup محفوظ على السيرفر: `/home/u176948796/backups/branch_backfill_backup_20260610112148.json`.
- الفحص المحلي:
  - npm audit: PASS للـ API/Admin/Client.
  - admin build: PASS.
  - client build: PASS.
  - API syntax: PASS على 42 ملف JS.
  - local Client/Admin/API live: PASS.
  - local DB readiness: BLOCKED لأن MySQL المحلي غير شغال `ECONNREFUSED 127.0.0.1:3306`.

## أرقام الإنتاج بعد الإصلاح

- المشتركين: 967.
- العملاء المحتملين: 11,107.
- المدفوعات: 145 بقيمة 305,150.
- الأوردرات: 5 كلها `PENDING` بقيمة 1,500.
- الكورسات: 22، المنشور منها 21.
- المحاضرات: 2,038.
- المسارات/الباقات: 7.
- الموظفين: 17.

توزيع المشتركين حسب الفرع بعد الإصلاح:

- بدون فرع: 912.
- `ONLINE_EGYPT`: 40.
- `DAQQI`: 9.
- `TAGAMOA`: 2.
- `ONLINE_ABROAD`: 1.
- `OTHER`: 3.

توزيع المدفوعات حسب الفرع بعد الإصلاح:

- بدون فرع: 125 دفعة / 265,766.
- `DAQQI`: 9 دفعات / 14,200.
- `ONLINE_EGYPT`: 9 دفعات / 15,985.
- `TAGAMOA`: 2 دفعات / 9,199.

## تقييم 30 جزء

| # | الجزء | التقييم | الحالة | ملاحظات QA |
|---|---|---:|---|---|
| 1 | استقرار الموقع العام | 9.0 | PASS | الرئيسية، الكورسات، auth، join-us، وAPI live تعمل بعد النشر. |
| 2 | تسجيل الدخول العام | 8.5 | PASS/RISK | صفحة auth تعمل. لم يتم تنفيذ تسجيل دخول حقيقي بحساب طالب في هذا المرور حتى لا نغير بيانات حقيقية. |
| 3 | صفحة الكورسات العامة | 8.8 | PASS | تعرض 21 برنامج، لكن يوجد رقم طلاب سلبي في أحد الكروت `-3 طالب` ويحتاج تنظيف داتا/حساب. |
| 4 | المسارات والباقات | 9.0 | PASS | صفحة الباقات تعمل وتعرض 7 مسارات. |
| 5 | صفحة الانضمام Join Us | 9.0 | PASS | كانت 404 على `/join-us` وتم إصلاحها ونشرها. |
| 6 | صفحة التواصل | 8.8 | PASS | النموذج ظاهر. لم يتم إرسال رسالة فعلية لتجنب side effect. |
| 7 | المجتمع العام | 8.0 | PASS/RISK | الصفحة تعمل لكن كل المحتوى تقريبا 0، وهذا ضعف محتوى لا كسر تقني. |
| 8 | صحة API الإنتاج | 9.5 | PASS | `/api/health/live` يعمل 200. |
| 9 | لوحة الإدارة عامة | 9.0 | PASS | التبويبات الرئيسية تفتح بدون Error Boundary. |
| 10 | العملاء المحتملون CRM | 8.7 | PASS/RISK | الصفحة تعمل وتعرض 9084 في الواجهة. بقي تضارب statuses uppercase/lowercase في DB يحتاج توحيد. |
| 11 | تحويل Lead إلى مشترك | 9.0 | PASS بعد الإصلاح | اختبرت Lead -> حجز/دفعة -> ظهور في الأونلاين، ثم حذفت بيانات QA. أصلحت تهيئة الفرع والإيميل في مودال الدفع. |
| 12 | فريق السيلز والمتابعة | 8.4 | PASS/RISK | الفلاتر والتوزيع ظاهرة، لكن يحتاج e2e لتأكيد تحديث كل حالات المتابعة والـ timeline. |
| 13 | عملاء الأونلاين | 9.0 | PASS | الصفحة تعرض 950 إجمالي بعد backfill، والتصنيفات تعمل. |
| 14 | التحصيل والأقساط | 8.4 | PASS/RISK | installment plans تظهر 1 عميل و5 خطط، لكن تحتاج تحقق عميق لسداد قسط وتحديث outstanding. |
| 15 | Online Hub | 8.5 | PASS | يعرض التحصيل والفريق، ولا يوجد crash. |
| 16 | قاعدة العملاء الموحدة | 8.6 | PASS/RISK | تعرض 9,996 إجمالي، لكن ما زال 912 مشترك بلا فرع وهذا يؤثر على التصنيفات. |
| 17 | جدول الدقي | 9.0 | PASS | بعد backfill أصبح `9 / 9` وتظهر الصفوف. |
| 18 | عملاء الدقي | 9.0 | PASS | تعرض 9 عملاء، بدون crash. |
| 19 | محاسبة الدقي | 9.1 | PASS | بعد fix وbackfill تعرض 9 مشترك و14,200 ج. |
| 20 | الحسابات العامة | 8.7 | PASS/RISK | تعرض 616,694.55 ج إجمالي إيرادات. الخطر: مصادر الداتا متعددة بين `payments`, `orders`, `paymentHistory`. |
| 21 | الطلبات والمدفوعات | 8.5 | PASS/RISK | 149 طلب/دفعة في الواجهة، payments كلها paid، orders كلها pending. يحتاج توحيد lifecycle. |
| 22 | الميزانية والتدفق النقدي | 8.3 | PASS/RISK | الصفحات تعمل، لكن دقة الأرقام تعتمد على اكتمال تصنيف الفروع والمصاريف. |
| 23 | الموارد البشرية | 8.8 | PASS | HR يعرض 17 موظف وملفات الأداء. |
| 24 | تذاكر الدعم | 8.7 | PASS | الصفحة تعمل، لكن لا توجد تذاكر فعلية لاختبار SLA end-to-end. |
| 25 | الاستردادات | 8.2 | PASS/RISK | تعرض طلبين. يحتاج اختبار دورة approve/reject وربطها بالمحاسبة. |
| 26 | الشهادات | 8.4 | PASS/RISK | 6 طلبات ظاهرة. يحتاج اختبار تغيير حالة شهادة حتى التسليم. |
| 27 | الاستشارات | 8.5 | PASS | التقويم يعمل، الداتا الحالية 0. |
| 28 | الكورسات والمحاضرات | 8.8 | PASS | 22 كورس و2038 محاضرة، صفحات الإدارة تعمل. |
| 29 | اللايف | 7.6 | PASS/RISK | صفحة الإدارة تعمل لكن لا توجد بثوث مباشرة، لذلك الوظيفة غير مثبتة فعليا. |
| 30 | الأمان والمراقبة والاختبارات | 8.4 | PASS/RISK | audits نظيفة، server monitor يعمل، لكن لا توجد suite e2e كاملة بعد. تم تحسين smoke ليصنف DB المحلي كـ BLOCKED بدل FAIL. |

## ما يعمل صح

- الموقع العام يفتح ويعرض الدورات والمسارات وتسجيل الدخول.
- لوحة الإدارة مستقرة بعد النشر ولا تظهر Error Boundary في الصفحات المختبرة.
- مسار Lead -> Payment -> Online Client يعمل عند إدخال بيانات الدفع.
- صفحات الدقي والحسابات تعرض بيانات حقيقية بعد إصلاح branch filtering/backfill.
- API live يعمل على الإنتاج.
- build للـ admin/client يعمل.
- npm audit نظيف.

## ما كان خطأ وتم إصلاحه

- `join-us` كان 404 على الجذر. تم إضافة route alias ونشر build صحيح للجذر.
- client كان hardcoded على `basename="/new/"` و`base="/new/"`. تم جعله configurable والافتراضي `/`.
- كثير من المشتركين والمدفوعات لم تكن مصنفة بفرع رغم وجود `lead_id` موثوق. تم backfill آمن.
- محاسبة الدقي كانت تفلتر `daqqi` ولا تطابق `DAQQI`. تم إصلاحها سابقا ونشرها.
- smoke test المحلي كان يعتبر DB down فشل كود. تم تعديله ليكون BLOCKED عند 503 readiness.

## ما لا يزال خطأ أو غير مكتمل

- 912 مشترك بلا فرع، لكن لا يوجد مصدر موثوق كاف الآن لتحديثهم بدون تخمين.
- 125 دفعة بلا فرع بقيمة 265,766، مرتبطة غالبا بمشتركين بلا فرع.
- حالات lead في DB ما زالت مختلطة: `NEW`, `CONVERTED`, وقيم lowercase. يجب توحيدها.
- صفحة المجتمع واللايف تعمل تقنيا، لكن المحتوى/الداتا شبه فارغة.
- Root/client deploy كان تاريخيا ملتبس بين `/` و`/new/`. تم إصلاح الكود والنشر، لكن يجب مراقبة الكاش.
- لا توجد e2e suite تغطي مسار العميل بالكامل.

## المسار الكامل للعميل - نتيجة المراجعة

1. زائر يدخل الموقع ويشاهد الكورسات والمسارات: PASS.
2. يضغط تفاصيل/تسجيل/اهتمام: لم يتم إرسال form حقيقي في هذا المرور لتجنب side effects، يحتاج test fixture.
3. Lead يصل CRM: تم اختبار إنشاء Lead سابقا بنجاح.
4. السيلز يحجز ويدخل دفعة: PASS بعد إصلاح تهيئة المودال.
5. يتحول إلى مشترك أونلاين/دقي حسب الفرع: PASS للسيناريو المختبر، وتحسن دقي بعد backfill.
6. يظهر في التحصيل/الأقساط: PASS جزئي، يحتاج اختبار دفع قسط فعلي.
7. يظهر في الحسابات: PASS، مع خطر تعدد مصادر الداتا.
8. يرى الكورسات في حسابه: لم يتم اختبار login طالب فعلي في هذا المرور.
9. الشهادات واللايف والمجتمع: واجهات تعمل، لكن flow كامل يحتاج fixtures.

## الإصلاحات المنفذة في هذا المرور

- `client/App.tsx`: إضافة route `/join-us` وجعل `Router basename` configurable.
- `client/vite.config.ts`: جعل `base` configurable والافتراضي `/`.
- `api/migrations/004_branch_backfill.sql`: migration موثق للـ branch backfill.
- `tools/mahad-api-smoke.mjs`: تصنيف DB readiness 503 كـ BLOCKED.
- `tools/mahad-quality-check.ps1`: دعم exit code 2 كـ BLOCKED.
- Production DB: Backfill آمن لـ 41 subscriber و18 payment.
- Production deploy: نشر client للجذر و`/new`، ورفع migration للإنتاج.

## الطريق إلى 10/10

- إنشاء test users/fixtures رسمية للطالب والسيلز والتحصيل والحسابات والدقي.
- Playwright e2e:
  - public interest form -> CRM lead.
  - lead booking/payment -> online client.
  - lead booking/payment DAQQI -> daqqi client + daqqi accounting.
  - installment payment -> outstanding -> collection dashboard.
  - certificate request -> pricing -> paid -> delivered.
  - student login -> course access -> lecture progress -> certificate eligibility.
- توحيد `LeadStatus` في DB/API/frontend إلى lowercase أو enum واحد.
- Backfill متقدم للـ 912 مشترك بلا فرع بعد تعريف قواعد موثوقة من source/course/payment method وليس تخمين.
- توحيد accounting source of truth: `payments` هو المصدر الأساسي، و`orders` للـ online checkout فقط.
- تقليل الملفات الكبيرة:
  - `Dashboard.tsx` ما زال 11,239 سطر.
  - `LeadsTab.tsx` ما زال 5,135 سطر.
  - `UnifiedClientPage.tsx` ما زال 4,054 سطر.
  - `core.js` ما زال 2,935 سطر.
- نقل runtime migrations المتبقية إلى migrations مرقمة.
- تحويل logs إلى logger موحد بمستويات.
- تقليل `console.*` و`innerHTML` و`as any`.

## ملحق ما بعد إصلاح 2026-06-10 12:15

تم تنفيذ قرار تحويل كل المشتركين الذين كانوا بلا فرع إلى فرع الأونلاين المحلي `ONLINE_EGYPT` بعد أخذ نسخة احتياطية على الإنتاج.

نتيجة تدقيق قاعدة البيانات بعد الإصلاح:

- المشتركون بلا فرع: `0`.
- المدفوعات بلا فرع: `0`.
- مشتركو `ONLINE_EGYPT`: `952`.
- مشتركو `DAQQI`: `9`.
- مدفوعات `ONLINE_EGYPT`: `134` بقيمة `281,751`.
- مدفوعات `DAQQI`: `9` بقيمة `14,200`.
- حالات العملاء المحتملين uppercase: `0` بعد توحيدها إلى lowercase.

اختبار فعلي من لوحة الإدارة بعد الإصلاح:

- العملاء المحتملون: الصفحة تفتح وتعرض جدول بيانات بدون Error Boundary وبدون أخطاء 502 في الكونسول.
- عملاء الأونلاين: يظهر `950` إجمالي، منهم `948` محلي و`2` دولي.
- قاعدة العملاء الموحدة: تظهر `9,998` إجمالي، `959` مشترك، `948` أونلاين، `11` دقي.
- جدول الدقي: يظهر `9 / 9`.
- محاسبة الدقي: تظهر `9` مشترك وإيرادات `14,200 ج.م`.
- الحسابات العامة: تظهر إيرادات `615,994.55 ج.م`.
- API الإنتاج: `/api/health/live` يرجع `200` محلياً وعبر الدومين.

تقييم الأقسام المتأثرة بعد الإصلاح:

- عملاء الأونلاين: `9.2/10` بعد إزالة سبب اختفاء العملاء غير المصنفين.
- جدول الدقي: `9.2/10` لأن العد والصفوف متطابقان.
- محاسبة الدقي: `9.2/10` لأن الفلترة والإجمالي يعملان على الداتا الفعلية.
- العملاء المحتملون CRM: `9.0/10` بعد توحيد حالات `LeadStatus` ومنع كتابة uppercase مستقبلاً.
- الحسابات العامة: `8.9/10`، تعمل وتعرض أرقاماً فعلية، لكن ما زال يلزم توحيد مصدر الحقيقة بين `payments` و`orders` و`paymentHistory` قبل تقييم 10/10.

ملاحظات متبقية تمنع التقييم الكامل 10/10:

- البريد على Hostinger يعرض `554 5.7.1 Disabled by user from hPanel`، وهذا يحتاج تفعيل البريد/SMTP من hPanel أو تغيير مزود الإرسال.
- لا توجد Playwright e2e suite كاملة بمستخدمين اختبار رسميين تغطي المسار كله من lead حتى شهادة/لايف.
- بعض ملفات الواجهة والـ API ما زالت أكبر من الهدف الصحي وتحتاج استمرار التفكيك التدريجي.
